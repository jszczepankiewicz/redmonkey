apply plugin: 'java'

sourceCompatibility = 1.8
version = '1.0'

repositories {
    mavenCentral()
}

allprojects{
    apply from: "$rootDir/dependencies.gradle"
}

dependencies {
    testCompile libraries.junit
}
